Write a python program to verify if the given input string, combinations of characters '{' and '}', is well formed.

s=input()
count=0
for ch in s:
    if ch=='{':
        count+=1
    elif ch=='}':
        count-=1
if count==0:
    print("combination is well formed")
else:
    print("combination is not well formed")
